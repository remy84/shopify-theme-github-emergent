<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="#0B0B0B">
    <link rel="canonical" href="{{ canonical_url }}">
    
    <title>
      {{ page_title }}
      {%- unless page_title contains 'We Have Faith' %} &ndash; We Have Faith{% endunless -%}
    </title>
    
    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}
    
    {{ content_for_header }}
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    {{ 'we-have-faith.css' | asset_url | stylesheet_tag }}

    <!-- Fallback CSS inline (au cas où l'asset ne se charge pas) -->
    <style>
      /* Fallback critique pour correspondre au site Emergent */
      :root { --bg: #0B0B0B; --text: #ffffff; --accent: #BFA05A; --accent-light: #F5EFE6; }
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif; background: var(--bg); color: var(--text); line-height: 1.6; overflow-x: hidden; }
      .container { max-width: 1200px; margin: 0 auto; padding: 0 1rem; }
      .site-header { position: fixed; top: 0; left: 0; right: 0; z-index: 1000; background: rgba(11,11,11,0.95); backdrop-filter: blur(10px); border-bottom: 1px solid rgba(255,255,255,0.1); height: 70px; }
      .announcement-bar { background: var(--accent); color: var(--bg); padding: 0.75rem 0; text-align: center; font-weight: 600; font-size: 0.9rem; }
      .hero { padding: 6rem 0 4rem; min-height: 80vh; display: flex; align-items: center; }
      .hero-title { font-size: 4.5rem; font-weight: 800; line-height: 1.05; margin-bottom: 2rem; letter-spacing: -0.02em; }
      .btn { display: inline-block; padding: 1rem 2rem; border-radius: 8px; font-weight: 600; text-decoration: none; transition: all 0.3s; border: none; cursor: pointer; font-size: 0.95rem; }
      .btn-primary { background: var(--accent); color: var(--bg); }
      .btn-primary:hover { background: #a68a4a; transform: translateY(-1px); }
    </style>
  </head>

  <body class="template-{{ template.name }}">
    <!-- Header -->
      <header class="fixed top-0 inset-x-0 z-50 backdrop-blur-[14px] bg-black/40 border-b border-white/10">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 h-16 grid grid-cols-[auto_1fr_auto] items-center gap-4">

    <!-- Logo -->
    <div class="flex items-center gap-3">
      <a href="/" class="inline-flex items-center gap-2" data-discover="true">
        <img src="https://customer-assets.emergentagent.com/job_wehave-faith/artifacts/2mrqhg65_LOGO%20WHF%20BLANC.png" alt="Logo We Have Faith" class="h-9 w-9 object-contain">
        <span class="text-white font-semibold tracking-[0.08em] uppercase text-sm">We Have Faith</span>
      </a>
    </div>

    <!-- Menu principal -->
    <div class="hidden md:flex items-center justify-center gap-3">
      <a href="/" class="px-3 py-2 text-sm tracking-wide text-[#BFA05A]" aria-current="page">Accueil</a>
      <nav aria-label="Main" class="relative z-10 flex max-w-max flex-1 items-center justify-center">
        <ul class="flex flex-1 list-none items-center justify-center space-x-1">
          <li>
            <button id="radix-:r0:-trigger-radix-:r1:" class="group inline-flex h-9 w-max items-center justify-center rounded-md px-4 py-2 text-sm font-medium bg-transparent text-white hover:text-white transition-colors">
              Homme
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="ml-1 h-3 w-3">
                <path d="m6 9 6 6 6-6"></path>
              </svg>
            </button>
          </li>
          <li>
            <button id="radix-:r0:-trigger-radix-:r2:" class="group inline-flex h-9 w-max items-center justify-center rounded-md px-4 py-2 text-sm font-medium bg-transparent text-white hover:text-white transition-colors">
              Femme
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="ml-1 h-3 w-3">
                <path d="m6 9 6 6 6-6"></path>
              </svg>
            </button>
          </li>
          <li>
            <button id="radix-:r0:-trigger-radix-:r3:" class="group inline-flex h-9 w-max items-center justify-center rounded-md px-4 py-2 text-sm font-medium bg-transparent text-white hover:text-white transition-colors">
              Enfant
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="ml-1 h-3 w-3">
                <path d="m6 9 6 6 6-6"></path>
              </svg>
            </button>
          </li>
          <li>
            <button id="radix-:r0:-trigger-radix-:r4:" class="group inline-flex h-9 w-max items-center justify-center rounded-md px-4 py-2 text-sm font-medium bg-transparent text-white hover:text-white transition-colors">
              À propos
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="ml-1 h-3 w-3">
                <path d="m6 9 6 6 6-6"></path>
              </svg>
            </button>
          </li>
        </ul>
      </nav>
      <a href="/my-shopify" class="px-3 py-2 text-sm tracking-wide text-white/80 hover:text-white">🏪 Shopify</a>
    </div>

    <!-- Actions utilisateur -->
    <div class="flex items-center justify-end gap-1">
      <button class="p-2 rounded-md hover:bg-white/10 text-white" aria-label="Rechercher">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="m21 21-4.34-4.34"></path>
          <circle cx="11" cy="11" r="8"></circle>
        </svg>
      </button>
      <button class="p-2 rounded-md hover:bg-white/10 text-white" aria-label="Connexion">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"></path>
          <circle cx="12" cy="7" r="4"></circle>
        </svg>
      </button>
      <button class="p-2 rounded-md hover:bg-white/10 text-white relative" aria-label="Panier">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="8" cy="21" r="1"></circle>
          <circle cx="19" cy="21" r="1"></circle>
          <path d="M2.05 2.05h2l2.66 12.42a2 2 0 0 0 2 1.58h9.78a2 2 0 0 0 1.95-1.57l1.65-7.43H5.12"></path>
        </svg>
      </button>
      <div class="md:hidden">
        <button class="p-2 rounded-md hover:bg-white/10 text-white" aria-label="Menu">
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>

  </div>
</header>

    <!-- Announcement Bar -->
    <div class="announcement-bar">
      Nouvelle collection disponible • Livraison gratuite dès 100€ • Retours sous 30 jours
    </div>

    <main id="MainContent" role="main">
      {{ content_for_layout }}
    </main>

    <!-- Footer -->
    <footer style="background-color: rgba(11,11,11,0.8); border-top: 1px solid rgba(245,239,230,0.1); padding: 3rem 0; margin-top: 4rem; text-align: center; color: rgba(255,255,255,0.7);">
      <div style="max-width: 1400px; margin: 0 auto; padding: 0 1.5rem;">
        <div style="font-family: Montserrat, sans-serif; font-size: 1.5rem; font-weight: 600; color: white; margin-bottom: 2rem;">We Have Faith</div>
        <div style="display: flex; justify-content: center; gap: 2rem; margin-bottom: 2rem; flex-wrap: wrap;">
          <a href="/pages/politique-vie-privee" style="color: rgba(255,255,255,0.7); text-decoration: none; font-size: 0.9rem;">Politique de confidentialité</a>
          <a href="/pages/contact" style="color: rgba(255,255,255,0.7); text-decoration: none; font-size: 0.9rem;">Contact</a>
        </div>
        <div style="color: rgba(255,255,255,0.5); font-size: 0.85rem; margin-top: 2rem; padding-top: 2rem; border-top: 1px solid rgba(245,239,230,0.1);">
          © {{ 'now' | date: "%Y" }} We Have Faith. Tous droits réservés.
        </div>
      </div>
    </footer>
  <!-- Cache refresh: 1755382759.8674955 -->
  </body>
</html>